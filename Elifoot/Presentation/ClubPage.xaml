<Page
  x:Class="Elifoot.Presentation.ClubPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Elifoot.Presentation"
  xmlns:model="using:Elifoot.Core.Entities"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:uen="using:Uno.Extensions.Navigation.UI"
  xmlns:utu="using:Uno.Toolkit.UI"
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance local:ClubModel}"
  NavigationCacheMode="Required"
  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <ScrollViewer IsTabStop="True">
    <Grid utu:SafeArea.Insets="VisibleBounds">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <utu:NavigationBar Content="{Binding Title}" />

      <Grid Grid.Row="1" Margin="10" >
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="5*" />
          <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" >
          <ListView ItemsSource="{Binding Players}">
            <ListView.ItemTemplate>
              <DataTemplate x:DataType="local:PlayerModel">
                <Grid ColumnSpacing="8" Padding="10">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="32" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>

                  <TextBlock Grid.Column="0" Text="{Binding Position}" />
                  <TextBlock Grid.Column="1" Text="{Binding Name}" />
                  <TextBlock Grid.Column="2" Text="{Binding Power}" />
                  <TextBlock Grid.Column="3" Text="{Binding Country}" />
                  <TextBlock Grid.Column="4" Text="{Binding Salary}" />
                </Grid>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackPanel>
        <TabView Grid.Column="1" CanReorderTabs="False" CanTearOutTabs="False" CanDragTabs="False" IsAddTabButtonVisible="False" TabWidthMode="SizeToContent" >
          <TabViewItem Header="Game" IsClosable="False">
            <TextBlock Text="HTEst" />
          </TabViewItem>
          <TabViewItem Header="Player" IsClosable="False" />
          <TabViewItem Header="Finance" IsClosable="False" />
          <TabViewItem Header="Selection" IsClosable="False" />
          <TabViewItem Header="Opponent" IsClosable="False" />
        </TabView>
      </Grid>
    </Grid>
  </ScrollViewer>
</Page>
